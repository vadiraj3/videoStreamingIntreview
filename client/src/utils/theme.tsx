// theme.ts

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	// Customize your theme here
	palette: {
		primary: {
			main: '#007bff', // Change to your primary color
		},
		secondary: {
			main: '#ff6f61', // Change to your secondary color
		},
	},
});

export default theme;
